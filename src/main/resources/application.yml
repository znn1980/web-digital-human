config:
  baidu:
    app-key:
    app-secret-key:
    access-key:
    access-secret-key:

server:
  port: 8080
  ssl:
    enabled: true
    key-store: classpath:keystore.jks
    key-store-password: 123456